#변수 age는 변수로 사용됨.
#변수에 값을 저장할 땐 = 을 사용. (할당의 의미)
#age = input('당신의 나이는?') #입력을 받고 age에 담는다.
my_int = 1
print(my_int) #1
my_int = my_int + 3
print(my_int) #4
my_int = my_int * 100
print(my_int) #400
#변수는 문자 대문자 소문자 모두 구분., 한글도 됨.
파이썬 = 1 #유니코드 방식을 사용하므로 특수문자와 한글 모두 가능하다.
이제열 = 42
print(파이썬)
print(이제열)
#숫자의 경우 문자 뒤에서만 선언이 가능하다. 1a (x) a1 (o)
#띄어쓰기 불가능하다.